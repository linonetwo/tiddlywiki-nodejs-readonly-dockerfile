apiVersion: v1
kind: Service
metadata:
  name: {{ include "tw.fullname" . }}
  labels:
    {{- include "tw.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.wiki.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "tw.selectorLabels" . | nindent 4 }}
